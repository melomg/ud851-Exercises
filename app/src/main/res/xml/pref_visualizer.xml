<?xml version="1.0" encoding="utf-8"?>

<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- COMPLETED (2) Create a res->bools.xml file and create a boolean for this default value -->
    <!-- COMPLETED (3) Update these values to be values from resource files and not hard coded -->

    <CheckBoxPreference
        android:defaultValue="@bool/pref_value_show_bass"
        android:key="@string/pref_key_show_bass"
        android:summaryOff="@string/pref_summary_off_show_bass"
        android:summaryOn="@string/pref_summary_on_show_bass"
        android:title="@string/pref_label_show_bass" />

</PreferenceScreen>
